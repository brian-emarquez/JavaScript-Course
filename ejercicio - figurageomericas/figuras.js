/*Edades*/

let figura = prompt("Introduce la digura geometica que quieres calcular el area: triangulo , rectangulo o curculo")

let base;
let height;
let radius;

switsh(figure){

    case 'triangulo':
        base = prompt("Introdice la base del triangulo")
        height = prompt("Introduce la altura del triangulo")
        console.log(`El area del triangulo es ${(base*height)/2}`);
        break

    case 'triangulo':
        base = prompt("Introdice la base del triangulo")
        height = prompt("Introduce la altura del triangulo")
        console.log(`El area del triangulo es ${(base*height)/2}`);
        break
       




}